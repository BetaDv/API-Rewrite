/**
 * 
 * @param {import("@betadv/easy-db")} database 
 * @returns 
 */
module.exports = (app, database) {
	return {
		name: "generate",
	type: "get",
	run: (req, res) => {
		// do something here
	}
	}
}